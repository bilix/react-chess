import styled from 'styled-components';

export const StyledHeader = styled.div`
    grid-area: header;
    align-self: center;
    justify-self: center;
    font-weight: bold;
    font-size: 25px;
`;